# /lib/content

Reserved for static/non-CMS copy (nav labels, footer legal text, About/
Privacy body copy) so nothing is hardcoded inside components — keeps the
door open for i18n later without restructuring (SDD §7.9).

Empty until Phase 2, when actual copy is written alongside the pages that
use it.
