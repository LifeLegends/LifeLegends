import styles from './KeyboardShortcutHint.module.css';

/**
 * KeyboardShortcutHint
 * Category: search
 * Status: SCAFFOLD ONLY — structure + typed props, no markup/animation yet.
 * Implemented in Phase 2+ per the approved SDD component inventory (§4).
 */

export interface KeyboardShortcutHintProps {
  className?: string;
}

export function KeyboardShortcutHint({ className }: KeyboardShortcutHintProps) {
  return null;
}

export default KeyboardShortcutHint;
