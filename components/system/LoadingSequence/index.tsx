import styles from './LoadingSequence.module.css';

/**
 * LoadingSequence
 * Category: system
 * Status: SCAFFOLD ONLY — structure + typed props, no markup/animation yet.
 * Implemented in Phase 2+ per the approved SDD component inventory (§4).
 */

export interface LoadingSequenceProps {
  className?: string;
}

export function LoadingSequence({ className }: LoadingSequenceProps) {
  return null;
}

export default LoadingSequence;
