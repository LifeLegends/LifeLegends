import styles from './LegendCard.module.css';

/**
 * LegendCard
 * Category: cards
 * Status: SCAFFOLD ONLY — structure + typed props, no markup/animation yet.
 * Implemented in Phase 2+ per the approved SDD component inventory (§4).
 */

export interface LegendCardProps {
  className?: string;
}

export function LegendCard({ className }: LegendCardProps) {
  return null;
}

export default LegendCard;
