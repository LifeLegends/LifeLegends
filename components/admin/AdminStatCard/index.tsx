import styles from './AdminStatCard.module.css';

/**
 * AdminStatCard
 * Category: admin
 * Status: SCAFFOLD ONLY — structure + typed props, no markup/animation yet.
 * Implemented in Phase 2+ per the approved SDD component inventory (§4).
 */

export interface AdminStatCardProps {
  className?: string;
}

export function AdminStatCard({ className }: AdminStatCardProps) {
  return null;
}

export default AdminStatCard;
