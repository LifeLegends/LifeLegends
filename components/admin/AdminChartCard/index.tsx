import styles from './AdminChartCard.module.css';

/**
 * AdminChartCard
 * Category: admin
 * Status: SCAFFOLD ONLY — structure + typed props, no markup/animation yet.
 * Implemented in Phase 2+ per the approved SDD component inventory (§4).
 */

export interface AdminChartCardProps {
  className?: string;
}

export function AdminChartCard({ className }: AdminChartCardProps) {
  return null;
}

export default AdminChartCard;
