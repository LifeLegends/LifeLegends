import styles from './LegendEditor.module.css';

/**
 * LegendEditor
 * Category: admin
 * Status: SCAFFOLD ONLY — structure + typed props, no markup/animation yet.
 * Implemented in Phase 2+ per the approved SDD component inventory (§4).
 */

export interface LegendEditorProps {
  className?: string;
}

export function LegendEditor({ className }: LegendEditorProps) {
  return null;
}

export default LegendEditor;
