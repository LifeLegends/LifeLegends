import styles from './AdminListCard.module.css';

/**
 * AdminListCard
 * Category: admin
 * Status: SCAFFOLD ONLY — structure + typed props, no markup/animation yet.
 * Implemented in Phase 2+ per the approved SDD component inventory (§4).
 */

export interface AdminListCardProps {
  className?: string;
}

export function AdminListCard({ className }: AdminListCardProps) {
  return null;
}

export default AdminListCard;
