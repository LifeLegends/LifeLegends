import styles from './BioTimelineNav.module.css';

/**
 * BioTimelineNav
 * Category: bio
 * Status: SCAFFOLD ONLY — structure + typed props, no markup/animation yet.
 * Implemented in Phase 2+ per the approved SDD component inventory (§4).
 */

export interface BioTimelineNavProps {
  className?: string;
}

export function BioTimelineNav({ className }: BioTimelineNavProps) {
  return null;
}

export default BioTimelineNav;
