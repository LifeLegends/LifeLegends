import styles from './BioHero.module.css';

/**
 * BioHero
 * Category: bio
 * Status: SCAFFOLD ONLY — structure + typed props, no markup/animation yet.
 * Implemented in Phase 2+ per the approved SDD component inventory (§4).
 */

export interface BioHeroProps {
  className?: string;
}

export function BioHero({ className }: BioHeroProps) {
  return null;
}

export default BioHero;
