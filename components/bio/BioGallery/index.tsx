import styles from './BioGallery.module.css';

/**
 * BioGallery
 * Category: bio
 * Status: SCAFFOLD ONLY — structure + typed props, no markup/animation yet.
 * Implemented in Phase 2+ per the approved SDD component inventory (§4).
 */

export interface BioGalleryProps {
  className?: string;
}

export function BioGallery({ className }: BioGalleryProps) {
  return null;
}

export default BioGallery;
