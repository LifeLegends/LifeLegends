import styles from './BioQuoteBlock.module.css';

/**
 * BioQuoteBlock
 * Category: bio
 * Status: SCAFFOLD ONLY — structure + typed props, no markup/animation yet.
 * Implemented in Phase 2+ per the approved SDD component inventory (§4).
 */

export interface BioQuoteBlockProps {
  className?: string;
}

export function BioQuoteBlock({ className }: BioQuoteBlockProps) {
  return null;
}

export default BioQuoteBlock;
