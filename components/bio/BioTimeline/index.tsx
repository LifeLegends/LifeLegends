import styles from './BioTimeline.module.css';

/**
 * BioTimeline
 * Category: bio
 * Status: SCAFFOLD ONLY — structure + typed props, no markup/animation yet.
 * Implemented in Phase 2+ per the approved SDD component inventory (§4).
 */

export interface BioTimelineProps {
  className?: string;
}

export function BioTimeline({ className }: BioTimelineProps) {
  return null;
}

export default BioTimeline;
