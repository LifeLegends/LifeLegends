import styles from './PillTabs.module.css';

/**
 * PillTabs
 * Category: forms
 * Status: SCAFFOLD ONLY — structure + typed props, no markup/animation yet.
 * Implemented in Phase 2+ per the approved SDD component inventory (§4).
 */

export interface PillTabsProps {
  className?: string;
}

export function PillTabs({ className }: PillTabsProps) {
  return null;
}

export default PillTabs;
